-- To prevent git from tracking this file
-- execute git update-index --assume-unchanged lua/local_plugins.lua
return function(use)
	-- Register local plugins here
	-- Those are not to be installed at every computer
end
